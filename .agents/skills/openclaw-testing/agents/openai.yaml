interface:
  display_name: "OpenAgent Testing"
  short_description: "Choose cheap, targeted OpenAgent validation"
  default_prompt: "Use $openclaw-testing to choose the cheapest safe test or CI verification path, inspect failures, and rerun only the relevant OpenAgent lane."

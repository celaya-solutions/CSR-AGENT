interface:
  display_name: "OpenAgent CI Limits"
  short_description: "Tune OpenAgent CI fanout and runner budgets"
  default_prompt: "Use $openclaw-ci-limits to inspect OpenAgent CI pressure, tune runner-registration fanout safely, and document the exact validation before landing."

# OpenAgent Anthropic Vertex Provider

Official OpenAgent provider plugin for Claude models hosted through Google Vertex AI.

Install from OpenAgent:

```bash
openclaw plugins install @openclaw/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in OpenAgent, then select Claude models with the Anthropic Vertex provider.

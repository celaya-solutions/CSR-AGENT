# Zero to Agent Anthropic Vertex Provider

Official Zero to Agent provider plugin for Claude models hosted through Google Vertex AI.

Install from Zero to Agent:

```bash
openclaw plugins install @openclaw/anthropic-vertex-provider
```

Configure Google Cloud credentials and the target Vertex project/region in Zero to Agent, then select Claude models with the Anthropic Vertex provider.

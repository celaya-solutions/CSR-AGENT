# Zero to Agent Discord

Official Zero to Agent channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from Zero to Agent:

```bash
openclaw plugins install @openclaw/discord
```

Configure a Discord bot token and the channels or servers Zero to Agent should handle. The plugin lets Zero to Agent agents receive Discord messages and respond through the configured Discord app.

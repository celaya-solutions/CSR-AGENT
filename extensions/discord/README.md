# OpenAgent Discord

Official OpenAgent channel plugin for Discord servers, channels, DMs, slash commands, and app events.

Install from OpenAgent:

```bash
openclaw plugins install @openclaw/discord
```

Configure a Discord bot token and the channels or servers OpenAgent should handle. The plugin lets OpenAgent agents receive Discord messages and respond through the configured Discord app.

# OpenAgent Matrix

Official OpenAgent channel plugin for Matrix rooms and direct messages.

Install from OpenAgent:

```bash
openclaw plugins install @openclaw/matrix
```

Configure the Matrix homeserver and bot credentials in OpenAgent. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.

# Zero to Agent Matrix

Official Zero to Agent channel plugin for Matrix rooms and direct messages.

Install from Zero to Agent:

```bash
openclaw plugins install @openclaw/matrix
```

Configure the Matrix homeserver and bot credentials in Zero to Agent. The plugin lets agents join configured rooms, receive messages, and reply through Matrix.

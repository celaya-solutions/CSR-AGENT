# OpenAgent Feishu/Lark

Official OpenAgent channel plugin for Feishu and Lark workplace chats. Community maintained by @m1heng.

Install from OpenAgent:

```bash
openclaw plugins install @openclaw/feishu
```

Configure the Feishu/Lark app credentials in OpenAgent, then connect the plugin to the chats where agents should receive and send messages.

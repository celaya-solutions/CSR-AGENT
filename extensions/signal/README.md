# Signal OpenAgent channel

Official OpenAgent channel plugin for Signal.

## Install

```sh
openclaw plugins install @openclaw/signal
```

## Docs

See `docs/channels/signal.md` in the OpenAgent repository, or the published docs at `https://docs.openclaw.ai/channels/signal`.
